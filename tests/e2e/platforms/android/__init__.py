# Android emulator E2E tests (TalkBack accessibility).
# Uses ADB to interact with the Android app running in an AVD emulator.
# Requires: Android SDK, AVD named "Pixel_7_API_34" (or update device name).
# Add tests here once the Android app exists in android/ or app/android/.
