# Playwright E2E tests for the web app (blind-assistant.org).
# Tests run against Chromium (NVDA+Chrome), Firefox, and WebKit (VoiceOver+Safari).
# Add tests here once the web app exists in website/ or src/web/.
