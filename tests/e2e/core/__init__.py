# End-to-end tests for full backend pipeline flows.
# These test the complete path: user input → orchestrator → tool → output.
# Only external APIs (Claude, ElevenLabs, Telegram servers) are mocked.
# Real encryption, real file I/O, real orchestrator logic runs.
