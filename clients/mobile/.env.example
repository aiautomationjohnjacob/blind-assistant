# Backend API URL (Expo public env vars are bundled into the app)
# Development: use localhost (Python backend running on your machine)
# Production: use your cloud deployment URL
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000
