# iOS Simulator E2E tests (VoiceOver accessibility).
# Uses xcrun simctl to interact with the iOS app running in the simulator.
# Requires: macOS + Xcode (iOS Simulator not available on Linux CI).
# macOS CI runner runs these on release tags only (see .github/workflows/ios-e2e.yml).
# Add tests here once the iOS app exists in ios/ or app/ios/.
